<%= render 'pages/home' %>

<% if user_signed_in? %>
	<div>
		<%= form_tag showforsale_path,  :method => 'get' do %>
		  <p>
		    <%= text_field_tag :search, params[:search] %>
		    <%= submit_tag "Search", :name => nil, class: "btn btn-primary" %>
		  </p>
		<% end %>
	</div>

	<div id="pins">
		<%= render @pins %>
	</div>
	
	<%= will_paginate @pins%>
<% end %>