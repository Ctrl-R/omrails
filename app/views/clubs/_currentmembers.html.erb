<div class="well">
	<table class="table table-striped">
		<% @club.userlist.each do |existinguser| %>
		<tr>
			<td width="40%"><%= link_to User.find(existinguser).name, user_path(existinguser) %></td>
			<% if @club.admin == current_user.id %>
				<td><% if @club.admin == existinguser %><% else %><%= link_to "Make Admin", changeadmin_club_path(@club, :changeadminuser => existinguser), :class => "btn btn-info", data: { confirm: 'Are you sure you want to make ' + User.find(existinguser).name + ' admin? You will no longer be admin for ' + @club.name + '.' } %><% end %></td>
				<td><% if @club.admin == existinguser %>Admin<% else %><%= link_to "Remove", removemember_club_path(@club, :removeuser => existinguser), :class => "btn btn-warning", data: { confirm: 'Are you sure you want to remove this user?' } %><% end %></td>
				<td><% if @club.admin == existinguser %><% else %><%= link_to "Ban", banmember_club_path(@club, :banuser => existinguser), :class => "btn btn-danger", data: { confirm: 'Are you sure you want to ban this user?' } %><% end %></td>
			<% end %>
		</tr>
		<% end %>
	</table>
</div>